# exact pins
# core agent: stdlib only — `python -m nexus_greed trade` needs nothing here

# server / command center (`serve`); lite + chaos harness only need websockets
fastapi==0.141.1
uvicorn[standard]==0.52.4
websockets==17.1

# unix only; windows stays on stock asyncio
uvloop==0.21.0; sys_platform != "win32"
